{
  "name": "recipeappserver",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start-win-dev": "set NODE_ENV=dev && node server",
    "start-win-prod": "set NODE_ENV=prod && node server",
    "start-unix-dev": "NODE_ENV=dev && node server",
    "start-unix-prod": "NODE_ENV=prod && node server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.9.4"
  }
}
